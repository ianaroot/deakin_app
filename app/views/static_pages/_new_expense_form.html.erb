<%= form_for expense do |f| %>
  <%= f.label :kind %>
  <%= f.text_field :amount, placeholder: "amount", id: "#{kind}_amount" %>
  <%= f.hidden_field :kind, value: kind %>
  <input id="#{kind}_date" name="expense[date]" type="date">
  <%= f.submit value: "record #{kind} purchase" %>
<% end %>